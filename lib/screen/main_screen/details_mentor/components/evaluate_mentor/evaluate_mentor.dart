import 'package:flutter/material.dart';

class EvaluateMentor extends StatelessWidget {
  const EvaluateMentor({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Evaluate Mentor'),
    );
  }
}
