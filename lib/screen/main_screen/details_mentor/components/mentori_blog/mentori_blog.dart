import 'package:flutter/material.dart';

class MentoriBlog extends StatelessWidget {
  const MentoriBlog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('mentori blog'),
    );
  }
}
