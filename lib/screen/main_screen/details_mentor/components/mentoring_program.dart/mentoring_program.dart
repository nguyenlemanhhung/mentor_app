import 'package:flutter/material.dart';

class MentoringProgram extends StatelessWidget {
  const MentoringProgram({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('mentoring program'),
    );
  }
}
