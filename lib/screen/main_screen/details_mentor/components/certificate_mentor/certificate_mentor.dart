import 'package:flutter/material.dart';

class CertificateMentor extends StatelessWidget {
  const CertificateMentor({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('chứng chỉ'),
    );
  }
}
